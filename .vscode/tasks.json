{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate ONNX strings",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "CUBLAS_WORKSPACE_CONFIG": ":4096:8",
                },
            },
            "command": "${command:python.interpreterPath}",
            "args": [
                "src/onnxnet/process/gen_onnx.py"
            ],
            "problemMatcher": [],
        }
    ],
    "inputs": []
}